import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  heading: {
    fontSize: 22,
    fontWeight: "600",
    color: "#fff",
    marginBottom: 4,
  },

  subHeading: {
    fontSize: 13,
    color: "#9a9a9a",
    marginBottom: 20,
  },

  emptyCard: {
    backgroundColor: "#151515",
    borderRadius: 16,
    paddingVertical: 40,
    alignItems: "center",
    borderWidth: 1,
    borderColor: "#1f1f1f",
  },

  icon: {
    marginBottom: 12,
  },

  emptyText: {
    fontSize: 14,
    color: "#9a9a9a",
  },
});
